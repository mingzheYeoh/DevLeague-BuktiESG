# Copy this file to .env at the repository root. Docker Compose and the API read it.
POSTGRES_PASSWORD=

# Optional. Only use with synthetic documents: document text is sent to
# api.openai.com when this key is set. Remove and rotate the key before
# uploading any real customer document.
# OPENAI_API_KEY=
