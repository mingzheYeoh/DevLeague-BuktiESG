# Base URL of the BuktiESG API (see backend/).
# The backend's CORS allow-list is localhost:3000 / 127.0.0.1:3000 by default,
# so serving this app from another origin means updating CORS_ALLOW_ORIGINS on
# the backend too.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
